import { UniqueCourseNameAsyncValidatorDirective } from './unique-course-name-async-validator.directive';

describe('UniqueCourseNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCourseNameAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
