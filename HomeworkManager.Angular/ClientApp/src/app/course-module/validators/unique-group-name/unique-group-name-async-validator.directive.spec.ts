import { UniqueGroupNameAsyncValidatorDirective } from './unique-group-name-async-validator.directive';

describe('UniqueGroupNameAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueGroupNameAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
