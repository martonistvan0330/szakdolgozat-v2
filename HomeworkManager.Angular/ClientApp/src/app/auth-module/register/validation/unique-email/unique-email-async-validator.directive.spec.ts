import { UniqueEmailAsyncValidatorDirective } from './unique-email-async-validator.directive';

describe('UniqueEmailAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEmailAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
