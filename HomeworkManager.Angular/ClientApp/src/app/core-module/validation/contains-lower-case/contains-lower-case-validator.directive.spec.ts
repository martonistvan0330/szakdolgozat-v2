import { ContainsLowerCaseValidatorDirective } from './contains-lower-case-validator.directive';

describe('ContainsLowerCaseValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainsLowerCaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
