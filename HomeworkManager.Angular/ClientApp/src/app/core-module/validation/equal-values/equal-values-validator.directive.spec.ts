import { EqualValuesValidatorDirective } from './equal-values-validator.directive';

describe('EqualValuesValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValuesValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
