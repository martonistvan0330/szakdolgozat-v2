import { ContainsUpperCaseValidatorDirective } from './contains-upper-case-validator.directive';

describe('ContainsUpperCaseValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainsUpperCaseValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
