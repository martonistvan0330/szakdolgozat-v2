import { ContainsDigitValidatorDirective } from './contains-digit-validator.directive';

describe('ContainsDigitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainsDigitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
