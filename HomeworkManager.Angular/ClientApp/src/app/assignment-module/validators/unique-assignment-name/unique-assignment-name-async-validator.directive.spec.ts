import { UniqueAssignmentNameAsyncValidatorDirective } from './unique-assignment-name-async-validator.directive';

describe('UniqueAssignmentNameAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueAssignmentNameAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
